# images/

Dossier vide pour l'instant. Assets à ajouter avant mise en ligne :

- `og-talox.jpg` (1200×630px) — image de partage réseaux sociaux, référencée dans les balises `og:image` / `twitter:image` des 8 pages (toutes pointent vers `https://talox.be/images/og-talox.jpg`, à créer avant mise en ligne)
- Le hero utilise une vidéo de fond (même source que talox.be, hébergée sur Pexels) : aucun asset local requis pour ça

Format recommandé : WebP, avec `loading="lazy"` sur toutes les images sauf celles visibles immédiatement au chargement.
