# images/

Dossier vide pour l'instant. Assets à ajouter avant mise en ligne :

- `og-talox.jpg` (1200×630px) — image de partage réseaux sociaux, référencée dans les balises `og:image` de `index.html`
- Toute image utilisée dans le hero ou les sections (actuellement le hero utilise un dégradé CSS, pas de vidéo/image de fond)

Format recommandé : WebP, avec `loading="lazy"` sur toutes les images sauf celles visibles immédiatement au chargement.
